// Live-data boundary for the 3D HQ.
// Do not place a Supabase service-role key in browser code.
// When ready, point this adapter to a safe API / Edge Function that returns aggregate KPIs.

export async function loadBusinessSnapshot() {
  return null
}
